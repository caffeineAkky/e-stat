{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Next.js: debug full stack (Webpack)",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/next/dist/bin/next",
      "runtimeArgs": ["--inspect"],
      "args": ["dev", "--webpack"],
      "skipFiles": ["<node_internals>/**"],
      "serverReadyAction": {
        "action": "debugWithChrome",   // 公式サンプルは debugWithEdge。Chrome 利用のため変更
        "killOnServerStop": true,
        "pattern": "- Local:.+(https?://.+)",
        "uriFormat": "%s",
        "webRoot": "${workspaceFolder}"
      }
    },
    {
      "name": "Next.js: debug server-side (Webpack)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev -- --inspect --webpack"
    },
    {
      "name": "Next.js: debug client-side",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000"
    }
  ]
}
